<Page x:Class="Beanfun.LoginWait"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Logging">
    <Page.Resources>
        <ResourceDictionary>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Background" Value="#7FF5F5F5"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>
            <Style TargetType="{x:Type TextBox}"/>
            <Style TargetType="{x:Type ListBox}"/>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <StackPanel Margin="0 30 0 0">
            <StackPanel Background="#7FFFFFFF">
                <StackPanel Margin="10">
                    <Label x:Name="t_Info" Content="{DynamicResource MsgLogging}" HorizontalAlignment="Center"/>
                    <Button Content="{DynamicResource Cancel}" Margin="0,5,0,0" Click="Button_Click" IsCancel="True"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
