<Window x:Class="Beanfun.AccRecovery"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Beanfun"
        mc:Ignorable="d"
        Title="{DynamicResource DataRecovery}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeToContent="WidthAndHeight">
    <Grid>
        <StackPanel>
            <DockPanel Margin="5">
                <Label Content="{DynamicResource Password}" VerticalAlignment="Center"/>
                <TextBox x:Name="t_Password" VerticalContentAlignment="Center" Width="200"/>
            </DockPanel>
            <DockPanel Margin="5">
                <Label Content="{DynamicResource Data}" VerticalAlignment="Center"/>
                <TextBox x:Name="t_Data" VerticalContentAlignment="Center" Width="200"/>
            </DockPanel>
            <DockPanel Margin="5">
                <Button Margin="5" Width="110" Content="{DynamicResource Export}" Click="Export_Button_Click"/>
                <Button Margin="5" Width="110" Content="{DynamicResource Recovery}" Click="Recovery_Button_Click"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
