<Window x:Class="Beanfun.ChangeServiceAccountDisplayName"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Beanfun"
        mc:Ignorable="d"
        Title="{DynamicResource ChangeAccountName}"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeToContent="WidthAndHeight">
    <Grid>
        <StackPanel>
            <DockPanel Margin="5">
                <Label Content="{DynamicResource ServiceAccountDisplayName}" VerticalAlignment="Center"/>
                <TextBox x:Name="txtNewServiceAccountDisplayName" VerticalContentAlignment="Center" Width="170"/>
            </DockPanel>
            <DockPanel Margin="5">
                <Button Margin="5" Width="110" Content="OK" Click="ButtonOk_Click" IsDefault="True"/>
                <Button Margin="5" Width="110" Content="{DynamicResource Cancel}" Click="ButtonCancel_Click" IsCancel="True"/>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
